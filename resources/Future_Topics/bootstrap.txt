Bootstrap notes:
   blocks eat up the whole line, in contrast inline lets them stay together
	float - floats component, similar to text align
Classes to remember:
fixed-top - sticks on the page no matter what
fixed-bottom
sticky-top sticks to the top when you reach it

npm init - to set up package.json in the root folder
npm install - to add dependencies, 
	--save to store locally
	--save-dev for local dev dependencies
npm install -g - for global

scss - precompiled css variables
gulp files - are like ant files for js files